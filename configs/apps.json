{
    "env": {
        "PATH": "$(PATH):$(HOME)/.local/bin"
    },
    "apps": [
        {
            "name": "Retroarch",
            "cmd": "/usr/bin/retroarch --config /retroarch/retroarch.cfg"
        }
    ]
}